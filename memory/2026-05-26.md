# 2026-05-26

## JCZQ tiered v2.1 复盘 + 落地（5/25 → 5/26）

- 5/25 是 tiered v2 首日生产，结果 4 张全输、14 腿仅 2 中、−¥100。原始数据落在 `.nutmeg-data/jczq/daily/2026-05-25/tiered-plan-review.{md,json}`。诊断三个结构性问题：A 档"压舱"假象（008/009/002 had 全 1.76+ 无 ≤1.50 触发 hhad cover，3 串 6.86× ≈ 大胆票）；BDE 高度耦合（`pick_lottery_tier` 显式 `_ = excluded` 让 E 与 BD 同场同向，5/25 实际有 5 场重复押注）；D "反大众" = 押让球反面 + 无 contrarian 有效性门控（今天大众站对 6/9 → 反咬）。
- 用户对齐：三条迭代方向全做；Q1 选 a（A 不出时首推降级到 D，不是 B、不是空）；Q2 选 a（E 跨档去重后腿不足直接不出、¥10 转 ¥0）。
- 落地 spec §25 三条硬约束：§25.1 A 真稳健化（`ANCHOR_REQUIRE_HOT_THRESHOLD=1.65` 池前置门、3 串 cap 5.5 / 2 串 cap 4.0、首推链 A→D→B→None）；§25.2 BDE 跨档去重（E 不再忽略 excluded、B↔D 同场反向 hhad 禁止、E 剩余腿 <4 返回 None）；§25.3 hhad 14d 健康度门控（让胜/让平/让负 ≥55% → D/E 剔除反向 hhad picks，B 不受影响，样本 <30 腿 disabled）。代码改：`nutmeg/services/jczq_tiered.py`（+`recent_hhad_market_health`、`PlanContext.hhad_health`、A 池前置 + per-fold cap、E 接 excluded、D 接 b_hhad_picks）+ `nutmeg/services/jczq_tiered_review.py`（`_day_record.by_hhad_actual` 累计 + `load_cross_version_history_dict` 加 `records` + `before_date` 防同日自指）。测试 31 → 51（新增 20 条），全量 pytest 0 失败。commit `a7b1db2`，spec 追加在 `docs/superpowers/specs/2026-05-25-jczq-tiered-plan-design.md` §25；memory 已落 `jczq_5_26_v2_1_landed.md`。
- 5/25 数据 retro 验证 v2.1：A 档不出（had min 1.76 > 1.65）→ 首推降级到 D（"A 档不出，本档为今晚最高优先级娱乐票"）→ E 档不出（BDE 互斥后无 4 腿空间）→ 总建议金额 ¥100 → ¥55，hhad 健康度公示「样本不足 7<30 腿 contrarian 正常开」。retro 复算 v2.1 在 5/25 仍输 −¥55（B 全错 + D 1/4），但去掉了 A 档假稳健 −¥35 的认知错觉。

## 2026-05-26 当日方案分析（v2.1 落地后首次正式生产）

- 跑 `nutmeg jczq-tiered`（live）：盘面 chaos 9/100 平静、7 场周二赛（005 弗拉门戈 vs 库斯科 had None 未开盘）；500.com 欧赔抓 2 场 fetch failed 但不影响核心通路。输出落在 `.nutmeg-data/jczq/daily/2026-05-26/tiered-plan.md`，总建议金额 ¥70（从默认 ¥100 自动降）。
- **A 稳健底仓（2 串 @3.31 · ¥35 · 首推一张）**：001 菲尔特 vs 埃森 胜 @1.75 + 007 大学体育 vs 托利马 胜 @1.89。诊断关键：A 没选更"热"的 003 拉努斯 had 1.65 / 006 蒙国民 had 1.60 / 004 基多体大 had 1.30，原因是 §17 体彩-欧赔 gap-guard 把 003/006 拒了（003 gap 10.06%、006 gap 11.56% 都超 8pp 安全线 = 体彩冲过欧赔太狠），004 had 1.30 触发 hhad cover @2.02 但 cover 进 candidates 后 3 串组合（001+007+004 = 6.68、001+007+002 = 6.75 等）全部超 §25.1 的 5.5 cap → 自动降 2 串 3.31 ≤ 4.0 ✓。v2.1 §25.1 has_real_favourite 通过的理由是 alt_info[004]=("had",1.30) ≤ 1.50（cover 触发即视作真热门池存在），即使最终选腿不含 004。
- **B 主方案（4 串 @148.90 · ¥35）**：004 基多体大 让平 @3.45 + 002 圣埃蒂安 让胜 @4.15 + 003 拉努斯 让胜 @3.25 + 006 蒙国民 让平 @3.20。B 不受 §17 gap-guard 约束（gap-guard 只守 A），把 003/006 这两场 gap-guard 在 A 拒掉的"体彩超买"场以 hhad 让球角度收回。设计意图是 A 谨慎 vs B 大胆并行，但要注意 B 这种"次级热门让球反面"4 腿命中率 raw 仅 ~0.7%。
- **D 档 / E 档不出**：A 用 {001,007}、B 用 {002,003,004,006}，A∪B excluded = {001,002,003,004,006,007} → 剩 005 一场（无开盘）→ D 候选空 → 不出；E 同理 BDE 互斥后 <4 腿 → 不出。这是 v2.1 §25.2 设计意图首次在生产里干净地触发：没有为了凑票数把 005（无开盘）硬塞、没有让 E 反向押 BD 已经覆盖的场。
- **hhad 健康度**：7 腿 < 30 门槛 → disabled、contrarian 暂裸跑。要到 ≥14 天攒齐 30 腿之后才会触发 dominance 剔除规则。
- 用户面诚实表态：A 1.75+1.89 名义上是"次级热门"组合，2 腿 raw 命中率 ~30%（远好于 5/25 v2 的 14.6%），且 001/007 是被 gap-guard 主动验证过的"体彩没冲过欧赔"的合理热门；如果只玩一张引擎建议 A。但 B 4 腿 148.90× 仍是高方差娱乐票、不是稳健票。
- 未推送 Telegram、未手动 dispatch；明日 08:00 launchd `bold-review-8am`（已切到 tiered-review）会自动跑次日复盘。

# 2026-05-28

## JCZQ tiered v2.3 复盘 + 落地（5/27 → 5/28）

- 5/27 是 v2.2 落地后首个完整 loop 复盘。dispatched 票（launchd 12:00 覆盖了我 11:18 手工派发的版本，md5 变化）：A 不出 / B 3 串 @71.40 ¥35 全 hhad+had / D 3 串 @129.03 ¥20 / E 不出。已结算 3/7 场（001 1:0 had 胜 hhad 让平 / 002 1:0 had 胜 hhad 让平 / 003 0:1 had 负 hhad 让负），4 场南美场未结算（004/005/006/007 仍待 okooo 刷新）。**v2.2 自动票腿 0/3 graded 命中，B+D 两张已死**（B 0/2、D 0/1，002 让胜 + 001 让胜 + 003 让平全错）。Claude finalize 立场（5/27 17:25 落盘 final-plan.json）graded 1/2（003 让负 ✅）；GPT 立场 A 2 串 hhad 让负预启用 §25.4 → 0/2 全错，E 5 串含 003 让胜 → 已死。
- 诊断 5 条结构性问题：(1) v2.2 D 选了 002 让胜，GPT direct Poisson edge=-22% 硬证据 — D 候选层完全没 edge 检查；(2) 003 强胆 1.30 主胜场，v2.2 B 选 hhad 让平@3.30 boldness 0.35 错，Claude finalize 选 hhad 让负@2.93 对 — boldness 排序在强胆场选错方向；(3) ttg 池空时 B 退化到 had 5.40 单点冷腿（5/27 ttg 整体为空，§26.2 ttg-min 优雅降级到 had）；(4) review 累计混算 v2/v2.1/v2.2 三版本 — 迭代分析时混淆基线；(5) 001/002 都 1:0 → hhad 让平，三个版本都没押让平，让 -1 + 主队最小胜的标准结果，需 14d 追踪。GPT §25.4 hhad 让球反面通道在 5/27 失手（A 2/2 全错）反而验证了 5/25 retro "等 10+ 天追踪" 纪律是对的；Claude 不预启用 §25.4 → 决策正确。
- 用户对齐：五条迭代方向全做；§27.2 强胆阈值 had ≤ 1.50（与 v2.1 ANCHOR_HHAD_COVER_THRESHOLD 一致）；§27.3 选项 c 公示警示 + 不动选腿；§27.5 加轻量 shadow JSON 字段；AGENTS.md / OpenClaw doc 改动仍不动（用户单独处理）。
- 落地 spec §27 五条规则：§27.1 D 候选 direct Poisson edge ≤ -0.15 硬过滤（新常量 `D_LEG_EDGE_HARD_FLOOR=-0.15` + 新公开函数 `compute_d_poisson_edge_index` + `select_tiered_plan`/`pick_contra_tier` 加可选 `poisson_edge_index` 参数 + CLI 接通）；§27.2 强胆场 hhad 让球反方向优先（新常量 `STRONG_FAV_HAD_THRESHOLD=1.50` + 内部 `_strong_fav_reverse_hhad` + `pick_main_tier` sort key 升级 hhad 反向 pick 优先 > 其它）；§27.3 ttg 池空 + B 出票时渲染层 ⚠️ 警示（`TieredPlan.ttg_pool_empty: bool` 字段 + render 顶部公示）；§27.4 review 累计 by_version 分组（`TieredPlan.version: str = "v2.3"`、markdown-restore 标 "v2.x"、`_cumulative` 加 `by_version` 聚合、render 加"按引擎版本分组"块 ≥2 版本时）；§27.5 让 -1 盘面 hhad 让平 shadow JSON（`_day_record` 加 `by_hhad_actual_let_neg_one` 字段 + `_try_load_match_hhad_lines` 从 snapshot 拿 hhad_line 映射）。代码改：`nutmeg/services/jczq_tiered.py` + `nutmeg/services/jczq_tiered_review.py` + `nutmeg/interfaces/cli/jczq.py`；测试新增 6 条（§27.1/§27.2/§27.3/§27.4/§27.5 都覆盖）。
- 端到端验证：v2.2 §26.4 markdown-restore 仍生效（5/26 review 显示原 B 59.42 不被 v2.3 规则改写）；全量 pytest 绿；live `nutmeg jczq-tiered` 跑过验证 Poisson edge index 传入路径。commit `pending`，spec 追加 §27；memory 已落 `jczq_5_28_v2_3_landed.md`。
- 5/27 待结算 4 场（004/005/006/007）今晚 okooo 刷新后再跑 review 看最终账；目前 v2.2 B+D 已死、Claude finalize B 已死 D 全待定、GPT A+E 已死 B/D 待定。
- AGENTS.md / docs/integrations/openclaw-nutmeg-agent-instruction.md 之前 session 的改动仍未提交（v2.2 / v2.3 两次都不动），用户后面单独处理。

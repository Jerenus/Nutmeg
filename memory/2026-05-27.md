# 2026-05-27

## JCZQ tiered v2.2 复盘 + 落地（5/26 → 5/27）

- 5/26 是 v2.1 首日正式生产。派发：A 2 串 @3.31 ¥35（001 胜 + 007 胜）+ B 3 串 @59.42 ¥35 全 hhad（004 让平 + 002 让胜 + 005 让平）+ D/E 不出。截至 5/27 复盘已知：001 胜 ✅（A 第 1 腿）/ 002 让负 ❌（B 第 2 腿坐实，B ¥35 输）/ 004 让平 ✅（B 第 1 腿）；007 / 005 / 006 仍待 okooo 刷新。**B ¥35 已确定输；A ¥35 待定**（007 若 ✅ A 整票回 ¥115.85，若 ❌ 则 A 也输）。原始数据落在 `.nutmeg-data/jczq/daily/2026-05-26/tiered-plan-review.{md,json}`，tiered-plan.md 在调试过程中被 `--replay` bug 覆盖过、随即手工还原。
- 诊断三个结构性问题 + 一个新发现的 bug：(1) **B 又出全 hhad** — 5/25 retro 分歧 4 已经诊断「B 应允许 ttg 中线腿混搭」但 §25 没动 B，5/26 重复犯（5/25 B 87.55× 全 hhad → 5/26 B 59.42× 全 hhad，pick_main_tier 排序里 hhad 和 ttg 同优先级 0 → hhad 数量多就压死 ttg）；(2) **005 弗拉门戈 had None 仍进 B 池** — A 走 `has_real_favourite` 过滤了它，但 v2_candidate_pool 没有同等"真有这个市场"的源头过滤；和 v1 generator 退役同源；(3) **D 在 calm 日若退化成 over-leveraged contrarian 仍会被首推** — 5/25 retro 分歧 5 的 D 4 串 177.27× 即此例，5/26 没触发但属潜在风险；(4) **`jczq-tiered --replay` 之前会无条件覆盖 `daily/<date>/tiered-plan.md`** — review 拿不到原始派发版，等于"今天的规则套到昨天的数据上自证清白"。
- 用户对齐：四条迭代方向全做；收尾之前 session 写到一半的"5/26 review gate"未提交改动；B 用"强制 ≥1 ttg 腿"修；005 类用 v2_candidate_pool 全局过滤修；v1 generator 今天一起加 deprecated banner 退役。
- 落地 spec §26 五条规则：§26.1 v2_candidate_pool had 可用性过滤（`_match_has_usable_had`）；§26.2.a sort key 升级 ttg<hhad<had<其它 + §26.2.b B 强制 ≥1 ttg 腿（ttg 池非空时；不存在则约束 disabled 优雅降级）+ §26.2.c `MAIN_MAX_HHAD_LEGS=2` / `CONTRA_MAX_HHAD_LEGS=2` + §26.2.d `MAIN_HHAD_HIGH_ODDS_CUTOFF=5.0`；§26.3 首推 D calm 安全网（chaos<20 + fold>3 / odds>180 / 全 hhad → 首推=None）；§26.4 review 从派发 markdown 还原（`_load_saved_markdown_plan`）+ CLI `if not replay:` 守卫；§26.5 v1 jczq-daily-advisor / jczq-daily-review 顶部打 ⚠️ DEPRECATED banner（不删代码、不动测试、不改 launchd）。
- 代码改：`nutmeg/services/jczq_tiered.py`（+`_match_has_usable_had` / v2_candidate_pool 入口过滤 / pick_main_tier sort key + ttg-min check / 收尾已有 hhad ≤2 + ≥5 cutoff + recommended_single_for_tiers）+ `nutmeg/services/jczq_tiered_review.py`（保留已有 `_load_saved_markdown_plan` + by_hhad_actual + before_date 防自指）+ `nutmeg/interfaces/cli/jczq.py`（`_V1_DEPRECATION_BANNER` + advisor/review 顶部打 + `if not replay:` 守卫 line 595）。测试 57 → 60（新增 3 条：had 过滤 + ttg-min 必含 + ttg 缺失优雅降级），全量 1002 pytest 绿。commit `fef2a73`，spec 追加 §26；memory 已落 `jczq_5_27_v2_2_landed.md`。
- 端到端验证：5/26 review markdown 还原后正确显示原 B 3 串 @59.42（002 让负 ❌、004 让平 ✅、005 待定），不是新规则下重跑的 D 104.02；`nutmeg jczq-tiered --replay 2026-05-26` 跑完 md5 不变（bug fix 生效）；`nutmeg jczq-daily-advisor --dry-run` 顶部打出 4 行 DEPRECATED banner；5/26 数据用 v2.2 新规则重跑（stdout only，不写盘）：A=001+007（同）+ B=不出（ttg-min 触发）+ D=104.02 出来了（A∪B 之外有空间）+ E=不出。
- 未推送 Telegram；明日 08:00 launchd `bold-review-8am`（已切到 tiered-review）会自动跑 5/27 复盘 — 届时拿到完整 okooo 结算后 A 票才能定结局。
- AGENTS.md / docs/integrations/openclaw-nutmeg-agent-instruction.md 两个文件是之前 session 留下的 OpenClaw coding-agent 备注（不在本次 v2.2 commit 范围），用户后面单独处理。

# 2026-04-24

- Completed Spec Kit slices `005-player-identity-materialization` and `006-prematch-snapshot-expansion`.
- Added player identity alignment, Transfermarkt/soccerdata local materialization, venue/weather cache, and `reference-refresh` CLI acceptance.
- Extended richer pre-match snapshot with environment context, grouped availability, bench depth, and matchup/trend sections.
- Added Open-Meteo + Nominatim fallback geocoding after discovering Open-Meteo search does not resolve stadium names like Emirates Stadium in live acceptance.
- Verified commands:
  - `uv run ruff check .`
  - `uv run pytest`
  - `python3 -m compileall nutmeg`
  - `bash scripts/verify.sh`
  - `uv run nutmeg reference-refresh --league epl --season 2025`
  - `uv run nutmeg seed-demo --league epl`
  - `uv run nutmeg fixture-snapshot --fixture-id epl-001 --format json`
- Live richer snapshot acceptance succeeded for cache-backed + weather/travel fields; API-Football-specific live fields remain unavailable without `NUTMEG_API_FOOTBALL_KEY`.
- Resumed live acceptance after configuring `NUTMEG_API_FOOTBALL_KEY`.
- Fixed API-Football `/injuries` duplication by deduplicating repeated provider rows inside `ApiFootballClient.fetch_fixture_injuries()`.
- Fixed venue/weather cache correctness for stadium geocoding: invalid cached country matches now trigger re-geocode + Nominatim fallback and weather-cache refresh.
- Added regression tests for injury dedupe, wrong-country cached venue refresh, and CLI no-key behavior when `.env` is present.
- Live acceptance re-verified on fixture `1379304` (`Liverpool vs Crystal Palace`): referee present (`A. Madley`), injuries unique (`8` home / `4` away), kickoff local time corrected to `2026-04-25 15:00:00+01:00`, head-to-head + splits + weather populated.
- Re-ran live `fixtures-sync` for EPL 14 days: `Synced 17 fixtures across 1 competition(s). requests=1`.
- Fresh verification after fixes: `uv run ruff check .`, `uv run pytest` (`49 passed`), `python3 -m compileall nutmeg`, `bash scripts/verify.sh`.
- Added Spec Kit slice `007-odds-snapshot` with spec/plan/research/data-model/contracts/tasks/verification artifacts.
- Implemented `odds-snapshot` CLI plus `OddsSnapshotService`, `nutmeg/domain/odds.py`, and API-Football pre-match odds normalization.
- Added no-vig fair probability + fair-odds aggregation for canonical markets: `match_winner`, `btts`, and `totals_2_5`.
- Live odds acceptance succeeded on fixture `1379304`: provider `api-football`, bookmaker_count `13`, `match_winner` / `btts` / `totals_2_5` all available with fair probabilities.
- Tightened odds provider normalization to recognize supported markets by stable market id even when provider display names vary.
- Residual risk recorded: DuckDB file locks can conflict when two CLI processes hit the same analytics DB concurrently.
- Fresh verification for the odds slice: `uv run ruff check .`, `uv run pytest` (`59 passed`), `python3 -m compileall nutmeg`, `bash scripts/verify.sh`.
- Implemented the next odds slice after `007-odds-snapshot`: The Odds API now has a real fixture-to-event reconciliation path instead of the previous truthful skeleton failure.
- Added configurable The Odds API sport-key mappings in `config/leagues.yaml`, cached `odds_provider_events` links in DuckDB, and canonical-market normalization for The Odds API `h2h`, `totals`, `btts`, and supported `spreads` lines.
- Targeted verification for the new slice: `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest tests/test_the_odds_api.py tests/test_odds_event_repository.py tests/test_odds_service.py -q` and `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest tests/test_api_football.py tests/test_the_odds_api.py tests/test_odds_event_repository.py tests/test_odds_history_repository.py tests/test_odds_service.py tests/test_cli.py -q`.

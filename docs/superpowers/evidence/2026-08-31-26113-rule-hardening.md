# 26113 Rule Hardening — Verification Evidence

**Branch:** `feat/26113-rule-hardening`
**Date:** 2026-08-31
**Plan:** `docs/superpowers/plans/2026-08-31-26113-rule-hardening.md`

## Commits from this execution

| Commit | Task | Scope |
|---|---|---|
| `9ca85b4` | Task 1 | `feat(decision): harden 26113 audit warnings` |
| `c6bc541` | Task 2 | `fix(decision): govern the rolling renjiu anchor` |
| `b15824b` | Task 3 | `feat(decision): compare fixed-bonus scale by break-even` |

Two commits authored by a concurrent session interleave with the above
(`5c8badd docs(spec): version the renjiu anchor policy`,
`c04508f docs(plan): execute 26113 rule hardening`). They are recorded here for
history only; nothing in this document claims them.

## Gate results (clean HEAD, isolated worktree)

Verified on a detached worktree at `b15824b` so concurrent working-tree edits
could not influence the result.

| Gate | Command | Result |
|---|---|---|
| Lint | `uv run ruff check .` | All checks passed |
| Compile | `uv run python -m compileall nutmeg tests` | OK |
| Suites | `uv run pytest tests/decision/ tests/ontology/ tests/product/ tests/analytics/ tests/migration/ -q` | all passed, 0 failed |

## Audit finding codes now emitted

`C9 opening_upset_double`, `C10 flagged_double_not_full`,
`C11 false_direction_band`, `C12 draw_underpriced_band`,
`crash_marker_off_lexicon`.

C11 and C12 intervals are half-open and pinned by boundary tests:
`gap12 ∈ [0.05, 0.10)` and `draw ∈ [0.29, 0.32)`.

## Read-only CLI replays

No file under `.nutmeg-data` was created or edited during replay; authored
fixtures live in the session scratchpad or `tests/fixtures/`.

**`decision-audit-legs`** (isolated three-leg fixture) → `0 ERROR / 5 WARN`:
`false_direction_band` and `flagged_double_not_full` on the 6.6pp leg,
`opening_upset_double` and `flagged_double_not_full` on the flagged anchor, and
`crash_marker_off_lexicon` on a leg carrying an unregistered marker — the
unknown name warns without activating C9.

**`zucai-optimize`** (isolated 26114 candidate fixture, `median_bonus_yuan`
supplied) → ranked ascending by break-even, `¥512最小` first:

| Version | Notes | Cost | P(all) | Break-even | Multiple |
|---|---|---|---|---|---|
| ¥512最小 | 256 | ¥512 | 9.06% | ¥5,652 | 1.82x |
| ¥768实出 | 384 | ¥768 | 13.34% | ¥5,759 | 1.85x |
| ¥1152加场6 | 576 | ¥1,152 | 19.37% | ¥5,948 | 1.91x |
| ¥1728再加场1 | 864 | ¥1,728 | 27.03% | ¥6,392 | 2.05x |

Selection order inverts the probability order, which is the point of the rule:
P rises 4.1x across this range while the break-even line rises monotonically.

**`zucai-deployment-gate`** (`tests/fixtures/zucai/26104-deployment-gate.json`
with the repository official-history fixture) → selected `M1296`,
break-even ¥6,097, median ¥6,446, ratio 0.95x, state `pass`, exit 0.

## Deviations from the plan

1. **Replay input for `decision-audit-legs`.** The plan names
   `.nutmeg-data/zucai/26113-legs.json`; that artifact does not exist, so an
   isolated three-leg fixture was authored in the scratchpad instead, per the
   plan's own fallback instruction.
2. **Historical replay expectations changed with the governed window.** Fixing
   the window at 12 moves the cohort median from ¥6,446 (14 rows) to ¥9,430
   (rows 26103–26092). Consequently `26103` now lands in `REVIEW` at 1.53x
   rather than `REDUCE_OR_EMPTY` at 2.24x, and `26104` reads 0.65x rather than
   0.95x. Tests were renamed and re-asserted to lock the new arithmetic; they do
   not re-endorse the original decisions.
3. **Break-even selection changed an existing test's expected candidate.**
   `test_gate_selects_..._and_reports_arithmetic` now selects `V288`
   (break-even ¥2,057) over `V384` (¥2,400); the test was renamed accordingly.
4. **Product window versioning is not in these commits.** A concurrent session
   is implementing `_renjiu_history_window` so pre-26113 issues keep the legacy
   `min(20, available_rows)` behaviour. That work is unstaged in the working
   tree and deliberately untouched here; commit `c6bc541` contains only the
   shared constant.

## Excluded files confirmed unstaged

`docs/nutmeg-scheduler-operations.md`, `scripts/openclaw/nutmeg_scheduler_ops.py`,
`tests/test_nutmeg_scheduler_ops.py`,
`docs/superpowers/plans/2026-08-28-codex-dev-handoff.md`, and
`scripts/render_zucai_26113_plan.py` were never staged or committed.

## Production safety

No ontology or scoreboard write, no Telegram dispatch, no launchd change, and no
ticket or deployment decision was made by this execution.

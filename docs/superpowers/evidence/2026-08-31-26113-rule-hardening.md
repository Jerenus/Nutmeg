# 26113 Rule Hardening - Verification Evidence

**Branch:** `feat/26113-rule-hardening`
**Date:** 2026-08-31
**Plan:** `docs/superpowers/plans/2026-08-31-26113-rule-hardening.md`

## Final implementation commits

| Commit | Scope |
|---|---|
| `9ca85b4` | C9-C12 audit warnings and closed crash-marker vocabulary |
| `c6bc541` | Initial official payout and rolling-window implementation |
| `8b59538` | Policy-versioned window correction and payout upper-bound correction |
| `b15824b` | Fixed-bonus optimizer and deployment break-even ranking |
| `3efcb7a` | Decimal sort key, cross-module agreement, and 26114 replay fixture |

Supporting design and plan commits are `709ed37`, `f6609f3`, `5c8badd`, and `c04508f`.
The preliminary evidence commit `114c0ca` was produced concurrently against an older tree;
this document supersedes its historical-window and product-query claims.

## TDD evidence

- C9-C12: 41 audit tests cover every trigger, full-cover non-trigger, exact half-open
  boundaries, legacy JSON, and unknown-marker reporting.
- Window policy RED: legacy 26104 replay was rejected by universal 12-row validation and the
  product helper did not exist (`3 failed`). GREEN preserves authored positive windows before
  26113 and requires exactly 12 from 26113.
- Payout boundary RED: a legal loss infinitesimally below CNY 1 per winning stake was rejected.
  GREEN applies epsilon only to the nonnegative lower bound and keeps the upper bound strictly
  below `stake_count`.
- Ranking RED: Decimal-distinct break-even values collapsed to equal floats and chose the lower
  cost candidate; the 26114 fixture was also absent (`2 failed`). GREEN sorts using Decimal,
  replays the fixture, and proves optimizer/deployment agreement.

## Verification gates

| Gate | Command | Result |
|---|---|---|
| Affected regression | `uv run pytest` on the seven affected decision/product files | 159 passed |
| Lint | `uv run ruff check .` | All checks passed |
| Compile | `uv run python -m compileall -q nutmeg tests` | exit 0 |
| Five domains | `uv run pytest tests/decision/ tests/ontology/ tests/product/ tests/analytics/ tests/migration/ -q` | 1,039 passed |
| Full repository | `uv run pytest -q` | 1,627 passed |
| Hooks | `uv run pre-commit run --all-files` | all hooks passed |

The pytest runs emitted only two third-party deprecation warnings from
`websockets.legacy`/uvicorn during the product browser test.

## Read-only CLI replays

`decision-audit-legs` on `.nutmeg-data/zucai/26113-legs-U1024.json`:

```text
出票前结构校验（26113）：0 个 ERROR / 8 个 WARN
```

The replay includes `flagged_double_not_full` and existing C6 findings. Unit fixtures separately
pin C9, C11, C12, and `crash_marker_off_lexicon`, including the rule that an unknown marker warns
without activating C9.

`zucai-optimize` on `tests/fixtures/zucai/26114-optimizer-break-even.json`:

```text
R256   128 notes   CNY 256    P=5.24%    break-even CNY 4,883   1.57x
R1728  864 notes   CNY 1,728  P=16.38%   break-even CNY 10,547  3.39x
帽内排序: R256 > R1728
```

This is deterministic reporting. It does not recommend or dispatch a ticket.

`zucai-deployment-gate` on the read-only 26113 gate with live official history:

```text
帽内最优: U1024 CNY 1,024 / cap CNY 1,600; P=16.70%
回本奖金 CNY 6,132; 12-issue median CNY 3,113; ratio 1.97x
state review (exit 2)
```

The policy-versioned 26104 offline replay remains reproducible:

```text
M1296 CNY 1,296; break-even CNY 6,097; 14-issue median CNY 6,446
ratio 0.95x; state pass (exit 0)
```

The already-running operator application returned HTTP 200 at `http://127.0.0.1:8790/`.

## Deviations from the plan

1. The planned `.nutmeg-data/zucai/26113-legs.json` does not exist. The read-only replay uses the
   existing `26113-legs-U1024.json` artifact instead.
2. `c6bc541` initially enforced 12 rows for every issue and rewrote 26103/26104 replay arithmetic.
   This contradicted the approved effective-issue spec and was corrected by `8b59538`; no history
   was rewritten in the final state.
3. The 26114 optimizer fixture uses a compact deterministic nine-leg scale comparison. It validates
   arithmetic and ranking without encoding a football judgment or ticket recommendation.
4. The implementation was interleaved with a concurrent Claude session. Every resulting commit was
   inspected, and all final claims above come from fresh verification on the current branch.

## Safety and excluded files

No ontology or scoreboard write, Telegram dispatch, launchd change, ticket confirmation, or funds
action was performed. The following user files remain unstaged and were never included in a commit:

- `docs/nutmeg-scheduler-operations.md`
- `scripts/openclaw/nutmeg_scheduler_ops.py`
- `tests/test_nutmeg_scheduler_ops.py`
- `docs/superpowers/plans/2026-08-28-codex-dev-handoff.md`
- `scripts/render_zucai_26113_plan.py`
